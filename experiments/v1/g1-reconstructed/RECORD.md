# ARC-MKII-V1 GLADIATOR — G1 Reconstructed Record

**Status:** `RECONSTRUCTED_FROM_FROZEN_G1_SPEC / CLOSED_NEGATIVE_RESULT / NO_TRAINING / NO_EVALUATION`

**Design/plan base:** `00878a24689ab993e6c6cc43d81ac2d8bce8fe9a`

## Provenance warning

This is **not** the original local G1 fossil and must not be represented as such.

The earlier local execution was reported as ending with `ARENA_UNIVERSE_INSUFFICIENT`, but that local worktree and its reported commit are not available in the current runtime and are not present on GitHub. Rather than recreate a lookalike commit and conflate provenance, this record independently reconstructs the deterministic G1 census from the already-frozen V1 design/plan rules.

The reconstruction was performed post-closure, without fitting LEARN or SCRAMBLED and without evaluating any V1 selector.

## Independently reconstructed census

| Stage | Count |
|---|---:|
| raw counters | 1,048,576 |
| grammar-valid presentations | 967,921 |
| matched 4/4 root ties | 408,442 |
| planner-gap presentations | 989 |
| frozen-feature representable | 989 |
| admitted presentations | 989 |
| canonical structural families | 40 |
| required threshold | 320 |

Therefore the frozen G1 gate is:

```text
ARENA_UNIVERSE_INSUFFICIENT -> STOP
```

No V1 TRAIN/EVAL split, protocol freeze, fitting, or scientific evaluation is authorized by this record.

## Reconstruction consistency

The reconstructed stage counts exactly match the counts reported by the earlier closed G1 execution:

```text
1,048,576 -> 967,921 -> 408,442 -> 989 -> 989 -> 40
```

The deterministic post-hoc reconstruction additionally recovered all 40 structural families for analysis. That family autopsy is exploratory and is deliberately not promoted into this closed G1 record.

Local reconstruction script SHA-256 at the time of this record:

```text
1ecc7cfe105e418b5d6a8e550ff9833233391529b92a70125f55a8c5d8e301de
```

The script itself is not committed here; this hash is only a provenance note for the temporary reconstruction used to cross-check the closed result.

## Claim ceiling

This record supports only:

> Under the frozen V1 Gladiator five-query generator and canonical-family criterion, the deterministic G1 universe contains 40 admitted canonical families, below the preregistered threshold of 320; therefore V1 does not proceed to training or evaluation.

It does not test whether informative feedback would have improved the frozen learner in a sufficiently diverse chamber.
