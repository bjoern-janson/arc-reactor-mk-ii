# G1 DIAGNOSTIC CUSTODY NOTE

**Status:** `KNOWN CONTEMPORANEOUS-CUSTODY OMISSION / G1 ACCEPTANCE UNAFFECTED`

The frozen September 9 V2 design prospectively requested exact family multiplicities as a structural-diversity diagnostic.

The official G1 custody fossil retains:

- aggregate admitted presentations: `984`;
- admitted canonical families: `436`;
- one first-admitted ordered presentation for each retained canonical family;
- the exact G1 `CENSUS.json` and `FAMILIES.jsonl` custody hashes.

It does **not** retain a per-family alias/multiplicity count.

The G1 gate used only:

```math
N_admitted_canonical >= 320.
```

The recorded result `436 >= 320` is therefore unaffected by the missing multiplicity diagnostic.

G2 must not regenerate the `2^20` confirmatory counter universe in order to fill this omission. It records the omission and proceeds only from the already fossilized 436 family records.

Any later reconstruction of family multiplicities must be labeled exactly in substance as:

```text
RETROSPECTIVE_DETERMINISTIC_RECONSTRUCTION
NOT_G1_EXECUTION_CUSTODY
NOT_USED_FOR_G1_ACCEPTANCE
NOT_USED_TO_CHANGE_V2
```

A reconstructible fact is not retroactively a contemporaneously fossilized fact.
